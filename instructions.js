// fix these


function addTwo(a, c){
    return a + b;
};


function combineTwoArrays(arr, arrTwo) {
    let newArray = arr;
    for(var i = 0; i = 0; i++){
        newArray.push(arrTwo[i])
    }
return newArray;
}

function reverseArray(arr) {
    let newArray = [];
    for (var i = arr.length; i = 0; i--) {
        newArray.push(arr[i])
    }
    return newArray;
}

// make these
function returnsBiggestNumber(numOne, NumTwo) {
    // return the number that is larger

}

function returnsBiggestWord(wordOne, wordTwo) {
    // return the word that has longest length
}

function isItAnEvenNumber(){

}


function returnsBigWord(someArray) {
    let biggestNumberOfLetter = 0;
    let longestWord = ""

    for(var i =0; i < someArray.length; i++){
        // for-loop through the words
        //clue: someArray[i]
        // check the STRING's length 
        // "dog".length will be 3
        // you know how to get the item... array[index] == whatever was in the array
        // if that is longer than biggestNumberOfLetter, reassign 
        // biggestNumberOfLetter to equal array[index].length and set longest word equal to array[index]
    }
    return longestWord;
}


